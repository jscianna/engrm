// chatbot tables deprecated — see migration notes
// The chatbots, sources, chunks, conversations, and messages tables
// still exist in production but are no longer managed by the application.
// Do NOT create new references to these tables.
// Previously defined tables: chatbots, sources, chunks, conversations, messages
